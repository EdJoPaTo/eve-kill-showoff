/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ModuloPipe } from './modulo.pipe';

describe('Pipe: Modulo', () => {
  it('create an instance', () => {
    let pipe = new ModuloPipe();
    expect(pipe).toBeTruthy();
  });
});
